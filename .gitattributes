###############################################################################
# 1. 换行符设置 (Line Endings)
# 这是最重要的部分。
###############################################################################

# 默认设置：
# - 自动检测文本文件
# - 检入(Commit)时强制转为 LF
# - 检出(Checkout)时，在 Windows 上转为 CRLF，在 Linux/Mac 上保持 LF
* text=auto

# 强制这些文件永远使用 LF (Linux 标准)
# 无论在 Windows 还是 Linux 上检出，都保持 LF。
# 这对 Docker 容器、Shell 脚本至关重要！
*.sh      text eol=lf
*.bash    text eol=lf
*.zsh     text eol=lf
*.py      text eol=lf
Dockerfile text eol=lf
docker-compose*.yml text eol=lf
*.js      text eol=lf
*.json    text eol=lf
*.css     text eol=lf
*.html    text eol=lf
*.md      text eol=lf

# 强制 Windows 批处理脚本使用 CRLF
*.bat     text eol=crlf
*.cmd     text eol=crlf
*.ps1     text eol=crlf

# 显式声明二进制文件 (防止 Git 尝试修改它们)
*.png     binary
*.jpg     binary
*.jpeg    binary
*.gif     binary
*.ico     binary
*.mov     binary
*.mp4     binary
*.mp3     binary
*.flv     binary
*.fla     binary
*.swf     binary
*.gz      binary
*.zip     binary
*.7z      binary
*.pyc     binary
*.pdf     binary

###############################################################################
# 2. GitHub 语言统计 (Linguist)
# 控制仓库语言条的显示
###############################################################################

# 忽略第三方库、生成文件或文档，不计入语言统计
# 例如：你不想让 html/css 占据 90% 的语言条，只想看 Python/Go
docs/** linguist-documentation
*.md                linguist-documentation
config/** linguist-vendored
assets/** linguist-vendored
dist/** linguist-generated
package-lock.json   linguist-generated
yarn.lock           linguist-generated

###############################################################################
# 3. 归档导出设置 (Export Ignore)
# 当你在 GitHub 页面点击 "Download ZIP" 时，忽略以下文件
###############################################################################

.gitattributes      export-ignore
.gitignore          export-ignore
.github/            export-ignore
.idea/              export-ignore
.vscode/            export-ignore
*.DS_Store          export-ignore
